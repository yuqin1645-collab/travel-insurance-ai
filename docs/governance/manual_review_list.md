# 人工审查案件清单

> 生成日期：2026-05-09（更新）
> 数据库一致率：64.2% (734/1143)
> 本清单列出AI无法自动修复、需要人工判断的案件

---

## 一、P2 剩余未修复案件（AI拒绝但人工通过，代码无法自动修复）

### 1. a0nC800000ILK9SIAX — 航班取消，EJU5270=U25270 同一航班，改签后航班号未提取

- **AI结论**：拒绝（延误0分钟）
- **人工结论**：赔付600元
- **问题**：EJU5270 是 easyJet Europe 代码，U25270 是 easyJet 主代码，两者是**同一班航班**（非改签前后关系）。该航班取消，飞常准确认无实际起降时间。材料中有登机牌显示 EJU5270，但乘客实际被改签到的**新航班号**未在材料中提取到。AI 无法计算延误时长。
- **修复尝试**：已实现航空公司代码别名映射（EJU/EZY/EZS→U2），飞常准可成功查询 EJU5270（通过别名 U25270），但返回结果仍为取消（同一航班）。
- **审查点**：确认材料中是否有改签后实际乘坐的新航班登机牌/证明，以及实际延误时长。

### 2. a0nC800000JkaghIAB — Vision 将原航班与替代航班颠倒，路线识别错误

- **AI结论**：通过（赔付300元）✅ 已修复
- **人工结论**：赔付300元
- **实际案情**：原行程 KTI→PVG（联程段），原航班 MU7256（计划 01:35→06:30）取消，航空公司安排保护航班 MU760，乘客实际乘坐 9C8534 KTI→PVG（实际出发 09:08，实际到达 13:47）。理赔路线是 **KTI→PVG**。
- **根因分析**：
  1. Vision 提取将 claim_focus 识别为 9C8534 **PVG→PNH**（被材料中"滞留金边一晚"误导），路线完全错误
  2. Vision 将实际乘坐的 9C8534 当作"原航班"，将真正被取消的 MU7256 当作"替代航班"——**原航班与替代航班颠倒**
  3. 飞常准查询 9C8534 返回正确路线 KTI→PVG，但与 parsed 路线 PVG→PNH 不匹配，`_merge_aviation_into_parsed` 拒绝写入 actual 时间
  4. `_compute_delay_minutes` 因缺少正确的 actual 时间无法计算，返回 `final_minutes: null`
  5. AI 模型基于文本描述独立判断"延误 5h10min，通过"
- **代码修复（2026-05-09）**：
  1. `utils.py` `_merge_aviation_into_parsed`：路线不匹配但飞常准有实际时间时，信任飞常准修正路线并写入 actual（解决 PVG→PNH 被纠正为 KTI→PVG）
  2. `alt_flight_lookup.py` `lookup_alt_flight_data`：保存 `alt_aviation_status` 到 `alternate_local`，供 delay_calc 判断替代航班是否取消
  3. `delay_calc.py` `_compute_delay_minutes`：新增「替代航班取消颠倒检测」——当主航班正常到达、替代航班取消时，自动将替代航班的计划时间作为原计划，清空 alt 时间让计算走口径3（计划→飞常准实际）
  4. `aviation_lookup.py` `lookup_aviation_data`：新增「原航班/替代航班颠倒检测」——在 `_merge_aviation_into_parsed` **之后**执行，当多个候选同路线且一个"已到达"一个"取消"，且取消航班计划时间较早时，将取消航班的计划时间写回 `schedule_local.planned_dep`，清空 alternate_local 和 schedule_revision_chain
- **修复验证（2026-05-09 18:27）**：
  - 交换检测成功触发：`检测到原航班/替代航班颠倒（9C8534已到达计划较晚，MU7256取消计划较早），已修正schedule_local.planned_dep=2026-02-23T01:35:00+07:00`
  - **延误计算：453分钟（7小时33分钟），赔付300元，审核通过** ✅
  - 计算逻辑：MU7256 计划 01:35 vs 9C8534 实际 09:08，延误 7h33min = 453分钟
  - **与人工结论一致**

### 3. a0nC800000I94QDIAZ — AI解析错误：将经停误判为联程改签

- **AI结论**：拒绝（延误105分钟）
- **人工结论**：赔付300元
- **问题**：MEL→PPP经BNE中转（非改签），AI将其标记为is_connecting_rebooking=true。alt_dep=alt_arr=13:45（同一时间，明显错误）。claimed_delay=305分钟但代码只算出105分钟。飞常准显示原航班准点（actual=planned），实际延误发生在经停/改签段。
- **审查点**：确认实际延误情况，是原航班延误还是经停/改签导致的延误。

### 4. a0nC800000JpHALIA3 — 中转接驳免责触发，但人工通过

- **AI结论**：拒绝（中转接驳免责：前序航班延误无法核实后续接驳情况）
- **人工结论**：赔付600元
- **问题**：LH727 PVG→MUC航班取消，AI标记为联程改签+missed_connection。hardcheck触发中转接驳免责直接拒赔。但computed_delay=1080分钟（文本提取），说明延误确实达到门槛。人工审核认为应该赔付。
- **审查点**：中转接驳免责的适用边界——航班取消改签场景是否应豁免此免责？

---

## 二、P1 出入境记录补件问题（26件，AI补件→人工通过/拒绝）

### 典型案件

| ForceID | 证件类型 | 路线 | 人工结论 | 问题 |
|---------|---------|------|---------|------|
| a0nC800000M4C18IAF | 身份证 | PVG→CDG | 300元 | 国际航班+旅行证件齐全，AI仍要求补出入境记录 |
| a0nC800000I7LyEIAV | 护照 | XMN→DPS | 超30天关案 | 护照+国际航班，AI仍要求补出入境记录 |
| a0nC800000IR10sIAD | 身份证 | BKK→HKG | 超30天关案 | 国际航班，AI仍要求补出入境记录 |

### 根因分析

`hardcheck.py` 的出入境记录兜底逻辑（第661-665行）：
- 条件：`is_international or airport_unknown` + `has_any_travel_doc`
- 但 `is_international` 依赖 `dep_info.get("country_code")` 和 `arr_info.get("country_code")`
- 当机场在数据库中找不到（如 CXR、BKK等非标准机场），`country_code` 为空，`is_international` 为 False
- 兜底条件 `airport_unknown` 需要 `not dep_found or not arr_found`，但有些机场found=True但country_code为空

### 审查点

这26件出入境记录补件案件，人工均未要求补件（直接通过或超30天关案）。需确认：
1. 身份证保单+国际航班的出入境记录是否必须？
2. 护照保单+国际航班是否可豁免出入境记录？
3. 当前兜底逻辑是否需要放宽？

---

## 三、P1 登机牌/行程单补件问题（16件）

AI要求补登机牌或电子客票行程单，但人工直接通过。典型场景：
- 飞常准已确认航班延误，AI仍要求补延误证明+登机牌
- 材料中有行程单但AI未识别

### 审查点

确认飞常准数据是否可替代登机牌/行程单作为"已登机"的证明。

---

## 四、AI通过/人工补件（27件，全部为"超30天未补件自动关案"）

这些案件AI判定通过并给出赔付金额，但人工系统显示"超30天未补件自动关案"。这不是AI的问题，而是人工处理流程的问题（案件在AI审核前已进入补件流程，AI审核后未更新人工状态）。

| 类型 | 数量 |
|------|------|
| 行李延误 | 13件 |
| 航班延误 | 14件 |

### 审查点

这些案件AI已通过，是否需要人工重新打开处理？

---

## 五、AI拒绝/人工补件（18件）

### 身份不匹配类（6件）

AI因"申请人姓名与保单被保人不一致"拒绝，人工要求补件。

| ForceID | AI拒绝原因 |
|---------|-----------|
| a0nC800000J4NkiIAF | 申请人=孙少杰，被保人=孙晓影 |
| a0nC800000L8cK9IAJ | 申请人=BAO YAXIAN，被保人=包亚仙 |
| a0nC800000LA267IAD | 申请人=黄德林，被保人=黄德林（可能是同一人但AI判断不一致） |
| a0nC800000ICDIUIA5 | 申请人=Y AOJUNJUN，被保人=姚军 |
| a0nC800000IjJXDIA3 | 申请人=刘伟华，被保人=刘伟华 |
| a0nC800000KN7A9IAL | 申请人=沈剑云，被保人=沈剑云 |

### 审查点

这些身份不匹配案件，是AI过于严格还是确实存在问题？特别是拼音姓名与中文姓名的匹配规则。

### 延误不足类（3件）

| ForceID | AI延误 | 人工结论 |
|---------|--------|---------|
| a0nC800000J24HYIAZ | 0分钟 | 超30天关案 |
| a0nC800000Io9owIAB | 159分钟 | 超30天关案 |
| a0nC800000LAKSnIAP | 39分钟 | 超30天关案 |

### 国内航班/区域不匹配类（4件）

AI因"纯国内航班"或"区域不匹配"拒绝，人工要求补件。

### 审查点

这些案件的区域判定是否正确？人工补件是否意味着人工认为可能赔付？

---

## 六、P0 案件（AI通过但人工拒绝）

**当前状态：0 件真实 P0（全部已修复）**

数据库中存在 5 条 `AI=通过/人工=拒绝` 记录，但全部为"取消理赔"（用户主动取消），非 AI 错误，脚本已自动排除。

### 已修复的 P0 案件

| ForceID | 问题 | 修复方式 | 结果 |
|---------|------|---------|------|
| a0nC800000Nx1EAIAZ | EK092 计划起飞时间在保单有效期外，AI 未识别 | 重跑后 `policy_coverage_check` 正确触发"超出有效期" | ✅ AI→拒绝，与人工一致 |
| a0nC800000NwnCpIAJ | 纯国内航班 PEK→NKG，AI 通过 | 重跑后正确识别为纯国内航班 | ✅ AI→拒绝，与人工一致 |

---

## 七、本次修复效果汇总（2026-05-09 更新）

| 修复项 | 修复前 | 修复后 | 状态 |
|--------|--------|--------|------|
| Nx1EAIAZ (超出有效期) | AI通过/人工拒绝 | AI拒绝/人工拒绝 | ✅ 已修复 |
| NwnCpIAJ (纯国内航班) | AI通过/人工拒绝 | AI拒绝/人工拒绝 | ✅ 已修复 |
| J6yD6IAJ (口径1回退) | 0分钟/拒绝 | 635分钟/通过 | ✅ 已修复 |
| IA9R4IAL (联程改签降级追溯) | 21分钟/拒绝 | 926分钟/通过 | ✅ 已修复 |
| HkY3NIAV (口径1回退扩展) | 0分钟/拒绝 | 1082分钟/通过 | ✅ 已修复 |
| ILK9SIAX (planned_dep unknown) | 0分钟/拒绝 | 0分钟/拒绝 | ❌ 需人工审查 |
| JkaghIAB (路线不匹配+航班颠倒) | 代码计算失败/AI通过 | 453分钟/通过(300元) | ✅ 已修复验证，与人工一致 |
| I94QDIAZ (AI解析错误) | 105分钟/拒绝 | 105分钟/拒绝 | ❌ 需人工审查 |
| JpHALIA3 (中转接驳免责) | 1080分钟/拒绝 | 1080分钟/拒绝 | ❌ 需人工审查 |
| K37StIAJ (中转接驳免责-新) | AI拒绝/人工通过 | AI拒绝/人工通过 | ❌ 需人工审查 |

**一致率变化**：59.9% → 66.2% → 64.2%（口径切换导致表面下降，实际代码修复有效）

### 新增待审查案件（2026-05-09）

| ForceID | 问题 | AI结论 | 人工结论 | 审查点 |
|---------|------|--------|---------|--------|
| a0nC800000K37StIAJ | 中转接驳免责触发 | 拒绝 | 通过（300元） | 航班取消改签场景下中转接驳免责是否应豁免？ |
