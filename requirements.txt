requests
python-dotenv

# 异步HTTP请求
aiohttp>=3.9.0

# 文档处理
pymupdf>=1.23.0
python-docx>=1.0.0
pdf2image>=1.16.0

# 报表与数据分析
pandas>=2.2.0
openpyxl>=3.1.0

# OCR服务依赖
# Tesseract本地OCR (推荐,免费)
pytesseract>=0.3.10
Pillow>=10.0.0

# 阿里云OCR (可选)
aliyun-python-sdk-core>=2.13.0
aliyun-python-sdk-ocr>=1.0.0

# 腾讯云OCR (可选)
tencentcloud-sdk-python>=3.0.0

# 百度OCR (可选)
baidu-aip>=4.16.0

# 异步MySQL驱动
aiomysql>=0.2.0

# 定时任务调度
APScheduler>=3.10.0

# 监控告警
sentry-sdk>=1.40.0
slack-sdk>=3.27.0
psutil>=5.9.0
